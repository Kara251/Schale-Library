# 生产构建期变量。NEXT_PUBLIC_* 会被内联进产物，因此必须在构建时就位，
# 不能放 wrangler vars。这里只放非机密值；PANEL_INTERNAL_TOKEN 走 wrangler secret put。
NEXT_PUBLIC_API_URL=https://api.bakivo.com
NEXT_PUBLIC_SITE_URL=https://www.bakivo.com
